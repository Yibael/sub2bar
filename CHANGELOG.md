# Changelog

版本采用 `MAJOR.MINOR.PATCH`，测试版可追加 `-alpha.N`、`-beta.N` 或 `-rc.N`。Git 标签为版本号加 `v` 前缀，主分支为 `master`。

## [Unreleased]

## [0.1.0-beta.2]

- “菜单栏账号”升级为“账号管理”，支持 Pin / OAuth 筛选、持久化调整 Pin 切换顺序，以及本地 OAuth 月订阅价格和每月续费日配置。
- 新增订阅周期实际消费与顶部 Pin 消费/订阅成本汇总，使用倍率后的 `actual_cost`；支持是否包含 Admin、统计时区和独立货币符号，只改变符号、不换算金额。配置不完整的账号不纳入两侧汇总，失败不冒充零或部分总额。
- 月周期按首尾均包含的日期显示，例如每月 15 日续费对应 9 月 15 日至 10 月 14 日；支持短月、闰年与原续费日恢复。消费独立每 30 秒查询本地数据库，不增加上游额度请求。
- 面板按 Pin 顺序通过左右按钮、方向键或横向拖动切换单个账号，不使用竖向滚动，展开详情直接撑高。周期、消费和成本压缩为同级指标；顶部不再显示参与 Pin / Admin 说明行，仍汇总全部符合条件的 Pin。
- 进入账号管理自动加载列表，1 分钟内复用内存缓存，过期后台刷新并保留旧列表；切换账号和调整顺序不发请求、不重置额度冷却。
- 已 Pin 账号显示“今日用量”，使用 `standard_cost` 随账号状态间隔批量刷新，仅查询本地使用日志；“本周计费”改为“本周用量”，保留原有窗口统计与估算口径。
- 修复手动刷新受倒计时限制的问题：点击立即发起普通额度查询，保持 `force=false`；进行中的请求不重复发送，完成后重新计算自动刷新倒计时。
- GitHub Release 标题仅显示版本号，不再添加应用名前缀；安装包名称保持不变。

## [0.1.0-beta.1]

对外测试版本从此编号开始；实际发布日期以 GitHub Release 为准。

- 账号统计移至列表上方；全员可调度时隐藏重复计数，有告警时显示“需关注”，读取失败和未知状态单独标明。底部仅保留连接状态、版本和操作按钮。
- 菜单栏面板底部在连接状态旁显示完整版本号 badge，保持中性配色，不显示内部构建号。
- 对外版本集中在 `VERSION`，设置页与安装包使用完整测试版版本；内部构建号自动生成，不在普通界面显示。测试版发布标记为 Pre-release，不占用 Latest。
- 额度刷新从请求完成后计时，跨面板开关、休眠和 Pin 变更保留冷却；关闭期间不请求，到期重开只刷新一次。保存间隔基于上次完成时间重算，取消请求和失败退避不会被重开绕过。
- 刷新改为账号状态、额度与费用两类请求，间隔分别可配置；额度支持 5 秒，批量查询始终使用 `force=false`，移除客户端固定 10 分钟预算。
- 修复保存刷新间隔清空缓存的问题：保留费用、周额度估算和账号列表，只在服务器或密钥改变时清理连接数据。
- 修复额度倒计时到期时的加载反馈：实际额度请求期间显示加载动画，至少持续 300ms；倒计时区域固定尺寸，刷新期间保持 `00:00`。
- 根据账号 `platform` 显示内置平台矢量图标，覆盖 OpenAI、Claude、Gemini 和 Antigravity；未知平台保留字母占位。
- 移除“管理 Pin”快捷入口；仅 Pin 超过 5 个时显示搜索和平台筛选。
- 面板底部改为简短连接状态，不再显示轮询频率和采样时效文案。
- 顶部加载动画与刷新图标在固定区域内互斥显示，仅额度刷新触发加载状态，并发及状态轮询不再触发。
- 已 Pin 账号的折叠卡片显示 5 小时 / 7 天额度重置倒计时，使用 `3d 2h` 等紧凑格式，本地更新不增加请求。
- 修复 Intel GitHub runner 的 Metal 渲染测试崩溃：CI 与 Release 仅排除该 runner 上的两项离屏渲染测试，Apple Silicon 和本地保留完整测试。

## [1.5.0]

仓库迁移时使用的开发编号，保留以下历史说明；后续对外版本采用上面的 `0.1.0-beta.1` 起始编号。

### 功能

- 原生 macOS 菜单栏监控、账号 Pin、并发汇总及额度展示。
- 连接 / 刷新 / 菜单栏账号三栏设置；保存配置与测试连接分离。
- 本地权限受限的凭据文件与进程内缓存，不依赖钥匙串授权。
- 并发 2 秒、状态 5 秒、额度快照可配置，高成本额度查询至少间隔 10 分钟。
- 关闭面板停止轮询，认证失败暂停，失败退避与迟到响应隔离。
- 中性面板内容背景；移除演示和断开连接入口。

### 工程与安全

- Swift 回归测试、文件权限和快照口径测试。
- 本地提交检查、Gitleaks 全历史扫描、Actions 语法检查。
- Apple Silicon / Intel CI 测试及通用应用打包。
- 标签触发的 GitHub Release、安装包 SHA-256 校验和、文档与贡献流程。
- 不提交或迁移历史构建产物、本地密钥、日志或审计工作文件。
