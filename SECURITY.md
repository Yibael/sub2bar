# 安全说明

## 报告安全问题

请不要在公开 Issue、PR、日志或截图中附带密钥、token、用户账号或可直接利用的敏感信息。

优先使用 GitHub 仓库 Security 页的私密漏洞报告入口（需维护者启用）。如果该入口尚不可用，请只提交不含漏洞细节和凭据的联系请求，以便确认私密沟通方式。

## 本地密钥的保护范围

Sub2Bar 将当前服务器的 Admin Key 以明文保存到当前用户的 Application Support 目录。目录权限为 `0700`，文件权限为 `0600`；拒绝符号链接和异常权限，使用权限受限的临时文件原子替换。运行期间复用内存缓存。

这不是加密保险库。同一用户运行的其他程序、管理员权限程序以及包含该文件的备份仍可能访问密钥。应用不以 Base64 或内置固定加密密钥冒充安全加密。Swift 字符串释放也不等同于内存安全擦除。

应用不读取或删除旧版钥匙串条目，不将 Admin Key 写进仓库、普通偏好设置、日志或发布包。不要把运行时凭据复制回工作区；也不要给 CI 配置 sub2api 管理员密钥。

## 网络与权限

- 默认 HTTPS，使用系统证书验证，不忽略无效证书。
- HTTP 需显式启用，会明文传输管理员密钥。
- 拒绝自动重定向，避免 `x-api-key` 泄漏到另一个地址。
- 只读监控不改变 Admin Key 在服务端的权限；这个密钥本身仍可能有管理写权限。
- 主动额度接口可能让 sub2api 发起上游请求，不能仅因客户端使用 GET 就认定没有副作用。

## 仓库和发布

`.gitignore`、暂存区检查和 CI 扫描只是多层防护，不保证发现所有自定义/混淆凭据。维护者还应开启分支保护、必要的 Review、Secret Scanning / Push Protection（如仓库支持），并保护版本标签的推送权限。

GitHub Actions 只使用内置临时 `GITHUB_TOKEN` 发布 Release。构建/测试任务是只读权限，仅发布任务拥有 `contents: write`。Actions 固定到 commit SHA；下载的审计工具固定版本并校验 SHA-256。

目前安装包为 ad-hoc 签名，未进行 Developer ID 签名或 Apple 公证。不要把工作流成功或 SHA-256 校验成功等同于 Apple 对应用的信任验证。工作流不修改本机安全策略。

如果怀疑真实密钥曾进入 Git 历史，应先在服务端撤销/轮换该密钥，再协调清理历史和副本；仅删除最新版本的文件并不足以处理历史泄漏。
